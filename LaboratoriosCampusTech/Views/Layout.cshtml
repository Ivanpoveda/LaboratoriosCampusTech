<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>LaboratoriosCampusTech - @ViewData["Title"]</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: linear-gradient(120deg,#f6f9ff,#e9fbff);
        }

        .hero {
            background: linear-gradient(90deg,#5b8cff,#7be0c6);
            color: white;
            padding: 2rem;
            border-radius: 1rem;
        }

        .card-glow {
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            border-radius: 1rem;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">CampusTech</a>

            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/Reservations">Listado</a></li>
                    <li class="nav-item"><a class="btn btn-primary text-white ms-3" href="/Reservations/Create">Reservar</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-4">
        <div class="hero mb-4">
            <h2>Gestión de Reservas - Laboratorios</h2>
            <p>Administra rápidamente las reservas de los laboratorios de cómputo.</p>
        </div>

        @RenderBody()
    </div>

    <footer class="text-center text-muted py-3">
        © LaboratoriosCampusTech @DateTime.Now.Year
    </footer>

</body>
</html>

